# version: '3'
# services:
#   web:
#     build:
#       context: .
#       dockerfile: Dockerfile
#       tags:
#         - myapp
#     ports:
#       - "80:80"
version: "3"
services:
  app:
    image: ankitraz/mywebsite:${TAG}
    ports:
      - "80:80"